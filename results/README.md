# results/

最終図（`.png`/`.pdf`）だけを置く。`.gitignore` で `results/` 配下の図のみ git 追跡される。
没図・中間データ（`.nc`/`.npz`）は置かない。

- 命名: `<解析名>_<内容>.png`
- 探索的な図で残す価値があるものは `results/exploratory/` へ。
- Obsidian からは `Research/Projects/pj1/Results(GitHub)/` （junction）で閲覧できる。
